export default class District{
    id: string
    name: string
    photoFilename: string

    constructor(id: string, name: string, photoFilename: string){
        this.id = id
        this.name = name,
        this.photoFilename = photoFilename
    }
}